{
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && npx prisma db seed && npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "installCommand": "npm install"
}
